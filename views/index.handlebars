<header>
<h1>No-Calorie Burgers</h1>
<img src="../assets/images/humberger.png" style="width:150px;">
</header>

<div class="burger-container">

<ul id="burgers-to-eat" class="burger-col">
  <h2>Burgers To Eat</h2>
  {{#each burgers}}
    {{#unless devoured}}
    <li>
      <span class="burger-name">{{burger_name}}</span>
      <button class="devour-button">EAT ME</button>
    </li>
    {{/unless}}
  {{/each}}
</ul>

<ul id="burgers-eaten" class="burger-col">
  <h2>Devoured Burgers</h2>
  {{#each burgers}}
    {{#if devoured}}
    <li>
      <span class="burger-name">{{burger_name}}</span>
    </li>
    {{/if}}
  {{/each}}
</ul>

</div>

<div class="burger-input">
  <h2>Input Your Burger</h2>
  <form class="burger-form">
    <textarea></textarea>
    <button type="submit" id="submit-btn">Submit</button>
  </form>
</div>
